import React from 'react';
import ReactDOM from 'react-dom/client'; // Обновление импорта
import App from './App';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root')); // Создание корня для рендеринга

root.render(<App />);
